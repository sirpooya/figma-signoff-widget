{
  "name": "Design-SignOff",
  "version": "1.0.0",
  "description": "Figma widget for design sign-off process",
  "scripts": {
    "test": "tsc -p src --noEmit",
    "build": "esbuild src/code.tsx --bundle --outfile=dist/code.js",
    "dev": "concurrently -n tsc,build 'npm run test -- --preserveWatchOutput --watch' 'npm run build -- --watch'"
  },
  "author": "",
  "license": "MIT",
  "keywords": [
    "figma",
    "widget",
    "design",
    "signoff"
  ],
  "dependencies": {
    "typescript": "^4.3.5"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.49.0",
    "@figma/widget-typings": "^1.4.0",
    "concurrently": "^6.4.0",
    "esbuild": "^0.13.15"
  }
}
